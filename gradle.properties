## Gradle Properties for AuraFrameFx
# Java 17 configuration
# org.gradle.java.home=C:\\Users\\Wehtt\\.jdks\\temurin-17.0.15
org.gradle.jvmargs=-Xmx6g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:MaxMetaspaceSize=1024m

# Enable build features
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true

# Enable configuration cache for performance
org.gradle.unsafe.configuration-cache=true
org.gradle.daemon=true

# Kotlin settings
kotlin.jvm.target.validation.mode=error
kotlin.incremental=true
kapt.incremental.apt=true
kotlin.code.style=official

# AndroidX settings
android.useAndroidX=true
android.enableJetifier=true

# Build features (enable only what's needed)
# Suppress warnings
android.suppressUnsupportedCompileSdk=33,34,35,36
systemProp.org.gradle.warning.mode=summary

# Enable build scan
buildScan.termsOfServiceUrl=https://gradle.com/terms-of-service
buildScan.termsOfServiceAgree=yes
org.jetbrains.dokka.experimental.gradle.pluginMode=V2EnabledWithHelpersl